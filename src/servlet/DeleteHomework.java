package servlet;

import java.io.IOException;
import java.io.PrintWriter;

import javax.servlet.ServletException;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

import Dao.HomeworkClass;
import Dao.HomeworkDao;
import Dao.HomeworkStudentDao;

public class DeleteHomework extends HttpServlet {

	/**
	 * Constructor of the object.
	 */
	public DeleteHomework() {
		super();
	}

	/**
	 * Destruction of the servlet. <br>
	 */
	public void destroy() {
		super.destroy(); // Just puts "destroy" string in log
		// Put your code here
	}

	/**
	 * The doGet method of the servlet. <br>
	 *
	 * This method is called when a form has its tag value method equals to get.
	 * 
	 * @param request the request send by the client to the server
	 * @param response the response send by the server to the client
	 * @throws ServletException if an error occurred
	 * @throws IOException if an error occurred
	 */
	public void doGet(HttpServletRequest request, HttpServletResponse response)
			throws ServletException, IOException {

		doPost(request, response);
	}

	/**
	 * The doPost method of the servlet. <br>
	 *
	 * This method is called when a form has its tag value method equals to post.
	 * 
	 * @param request the request send by the client to the server
	 * @param response the response send by the server to the client
	 * @throws ServletException if an error occurred
	 * @throws IOException if an error occurred
	 */
	public void doPost(HttpServletRequest request, HttpServletResponse response)
			throws ServletException, IOException {

		request.setCharacterEncoding("utf-8");
		HomeworkDao homework = new HomeworkDao();
		HomeworkStudentDao hsd=new HomeworkStudentDao();
		HomeworkClass hc = new HomeworkClass();
		homework.deleteByHomeworkId((String)request.getParameter("homeworkId"));
		hsd.deleteByHomeworkId((String)request.getParameter("homeworkId"));
		hc.deleteHomeworkClass(Integer.parseInt(request.getParameter("homeworkId")));
		response.sendRedirect("ListHomeworksByTeacherId?teacherId="+(String)request.getParameter("teacherId"));
	}

	/**
	 * Initialization of the servlet. <br>
	 *
	 * @throws ServletException if an error occurs
	 */
	public void init() throws ServletException {
		// Put your code here
	}

}
